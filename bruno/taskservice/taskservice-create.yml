info:
  name: taskservice-create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/tasks
  body:
    type: json
    data: |
      {
        "title": "Estudar Spring",
        "description": "Revisar JPA e Transactions"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
