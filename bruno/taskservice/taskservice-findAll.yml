info:
  name: taskservice-findAll
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/tasks
  body:
    type: json
    data: |
      {
        "title": "Estudar Spring",
        "description": "Revisar JPA e Transactions"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
